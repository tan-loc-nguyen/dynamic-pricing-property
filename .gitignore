# Secrets — never commit
.env
.env.local
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
.next/
out/
npm-debug.log*
.pnpm-debug.log*

# Database (regenerate with `make reseed`)
data/*.db
data/*.db-shm
data/*.db-wal

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Build artefacts
tsconfig.tsbuildinfo

# Packaged desktop build (`make bundle`) — an 11MB binary per platform
dist/
build/

# Client source documents — NEVER commit (repo is public)
private/
*.pdf
